# Replay Case 005 Assets

This replay case preserves the source WXML/WXSS/JS from the lab golden case.

The current copied triple keeps its original dynamic image references in JS.
Screenshots from the verified lab run are stored in `../screenshots/`.

Do not copy the whole LAB asset pipeline into ORIG_REPO for this baseline.
